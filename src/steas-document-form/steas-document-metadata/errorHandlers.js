const handleDictionaryError = ({action}) => {
    
    
}
const handleReferencesFetchedError= ({action}) =>{
    //console.log('handleReferencesFetchedError', action)
}

module.exports = {
    handleDictionaryError,
    handleReferencesFetchedError
}